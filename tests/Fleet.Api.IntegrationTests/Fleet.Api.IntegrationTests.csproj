<Project Sdk="Microsoft.NET.Sdk">

  <!--
    End-to-end tests for the reference slice: real HTTP over a real host.

    Milestone 1 ships the host smoke test only. The Testcontainers Postgres fixture, and with it
    the parallel double-booking test, arrive with the endpoints in milestone 6.
  -->
  <PropertyGroup>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>Fleet.Api.IntegrationTests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Fleet.Api\Fleet.Api.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
  </ItemGroup>

</Project>
