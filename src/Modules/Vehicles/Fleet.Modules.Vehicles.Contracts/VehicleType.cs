namespace Fleet.Modules.Vehicles.Contracts;

/// <summary>What kind of vehicle this is. Fixed set; the fleet does not buy anything else.</summary>
public enum VehicleType
{
    Car = 1,
    Van = 2,
    Truck = 3,
    Bus = 4,
}
