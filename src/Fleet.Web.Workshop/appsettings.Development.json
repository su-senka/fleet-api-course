{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore.Authentication": "Debug"
    }
  },

  "Spa": {
    "//": "Keycloak always redirects back to this host, not to Vite - it has no reverse proxy to see through. In production the BFF serves the SPA itself, so this is empty and every redirect stays relative.",
    "DevServerOrigin": "http://localhost:5174"
  }
}
